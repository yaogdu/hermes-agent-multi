"""Control Panel reusable modules — auth, users, group owners, migrations.

All functions accept ``db_path: Path`` as the first parameter so they can
be used from the standalone Control Panel server or from the Hermes Dashboard
web-server integration.
"""
